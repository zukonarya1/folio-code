[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = .
markers =
    frontend: Frontend behavioral tests (require running dev server)
    safety: Safety tests that detect infinite loops and resource exhaustion
    slow: Tests that take more than 5 seconds
